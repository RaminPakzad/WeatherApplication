spring:
  datasource:
    driver-class-name: org.h2.Driver
    username: sa
    password:
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
  jpa:
    hibernate:
      ddl-auto: create-drop

weather:
  api:
    url: >
      http://dummy-url/{city}/{appid}
    app:
      id: app_id
